# Cover Letter

## 업무상 강점
저의 핵심 강점은 문제를 단계와 기준으로 다시 나누는 능력입니다.

저는 기능을 하나 더 붙이는 것보다, 먼저 어디서 흐름이 끊기는지, 어떤 조건에서 판단이 갈리는지, 무엇을 남겨야 다시 비교하고 고칠 수 있는지부터 봅니다. 이 방식은 AI를 공부한 뒤에 새로 생긴 것이 아니라, 창업과 외식업 운영, 프랜차이즈 현장에서 먼저 만들어졌습니다.

이 관점은 AI 프로젝트에서도 그대로 이어졌습니다. `trade-onboarding-agent`에서는 무역 실무 온보딩 프로젝트 안에서 `RAG`와 `riskmanaging agent` 흐름을 중심으로 작업했습니다. `LGEA`에서는 persona에 따라 모델의 guardrail이 어느 표면에서 무너지는지 비교하는 평가 워크스페이스를 다뤘습니다. `POMMIT`에서는 `service_agent` 흐름 안에서 HTML 정리, 텍스트 정제, 단어 빈도 추출, collector 전달 구조를 다뤘고, 이후 HybridRAG와 NLP 전처리 강화 방향은 연구 메모와 설계 문서 수준에서 검토하고 문서화했습니다.

## 동기 및 포부
저는 AI를 단순히 새로운 기능을 붙이는 수단으로 보지 않습니다. 실제 사용자 문제를 해결하려면 입력이 어떤 상태를 만들고, 그 상태가 어떤 판단과 분기로 이어지며, 마지막에 어떤 결과와 검증 구조를 남겨야 하는지까지 설계해야 한다고 생각합니다.

제가 하고 싶은 일은 모델을 한 번 연결해 보는 일이 아니라, 실제로 돌아가고 다시 고칠 수 있는 구조를 만드는 일입니다. `trade-onboarding-agent`에서는 도메인 RAG와 riskmanaging 흐름을, `LGEA`에서는 persona 기반 가드레일 붕괴 테스트 구조를, `poketdogam`에서는 멀티모달 입력과 fallback, trace를 포함한 사용자 흐름을, `LUMI`에서는 서비스 구조와 아키텍처, 배포와 LLMOps를 학습할 수 있는 본체를 다뤘습니다.

## 준비 노력
SESAC x UPSTAGE 과정에서 Python, SQL, RAG, 프롬프트 엔지니어링, 프로젝트 기반 협업을 학습하고 있고, 동시에 공개 저장소에서 AI Agent, 운영형 백엔드, 평가 구조를 직접 다루고 있습니다. 단순히 강의를 듣고 끝내지 않기 위해 `trade-onboarding-agent`, `LGEA`, `poketdogam`, `LUMI`, `POMMIT` 같은 프로젝트를 통해 어떤 입력이 어떤 처리 단계로 이어지는지, 어디에 fallback이 필요한지, 무엇을 파일과 trace로 남겨야 하는지를 계속 손으로 확인하고 있습니다.